<% provide :title, "Show lecturer" %>

<% if user_signed_in? %>
  <% content_for :left_info do %>
    <section class="user_info">
      <%= render 'shared/user_info' %>
    </section>
  <% end %>
<% end %>

Username : <%= @user.name %>
Email : <%= @user.email %>
Subjects = <%= @subjects %>